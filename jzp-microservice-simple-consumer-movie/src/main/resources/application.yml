server:
  port: 8010

info:
  app:
    # 使用@properties.name@ 取pom中对应标签的值
    name: @project.artifactId@
    encoding: @project.build.sourceEncoding@
    java:
      source: @java.version@
      target: @java.version@
